export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
} as const;

/** ~14px base radius per v0 spec (--radius: 0.875rem). */
export const radii = {
  sm: 10,
  md: 14,
  lg: 16,
  xl: 20,
  full: 999,
} as const;
