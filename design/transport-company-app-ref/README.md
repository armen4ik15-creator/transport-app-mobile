# Design reference: transport-company-app

Источник: `transport-company-app.zip` (v0 / Next.js прототип).

Используется **только как визуальный референс** — не запускать в production.

## Перенесено в React Native (`src/`)

| Референс (CSS) | RN токен |
|----------------|----------|
| `--background` #0f172a | `colors.bg` |
| `--card` #1e293b | `colors.surface` |
| `--card-elevated` #273449 | `colors.surfaceElevated` |
| `--primary` #3b82f6 | `colors.primary` |
| `--success` #10b981 | `colors.profit` |
| `--destructive` #ef4444 | `colors.loss` |
| `--warning` #f59e0b | `colors.warning` |
| `--foreground` #f1f5f9 | `colors.text` |
| `--muted-foreground` #94a3b8 | `colors.textMuted` |
| `--border` #334155 | `colors.border` |

Компоненты: `CustomBottomTabBar`, `ScreenHeader`, `ui-kit/PrimaryBanner`.
